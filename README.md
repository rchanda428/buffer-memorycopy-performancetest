# buffer-memorycopy-performancetest
this is a simple c program which can be used to test memory copy time for raw image buffers, it takes one yuv format input and copy to other mem location created by malloc
